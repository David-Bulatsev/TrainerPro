[pytest]
pythonpath = .
testpaths = tests
addopts = --strict-markers --cov=app --cov-report=term-missing --cov-fail-under=60
asyncio_default_fixture_loop_scope = function
markers =
    unit: fast isolated tests
    integration: api and db integration tests
    slow: longer running scenarios
